{
  "name": "compras-na-25-backend",
  "version": "1.0.0",
  "description": "Fastify API for Compras na 25",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "migrate": "prisma migrate dev",
    "migrate:prod": "prisma migrate deploy"
  },
  "dependencies": {
    "fastify": "latest",
    "@fastify/cors": "latest",
    "prisma": "latest",
    "@prisma/client": "latest",
    "dotenv": "latest"
  },
  "devDependencies": {
    "typescript": "latest",
    "@types/node": "latest",
    "tsx": "latest"
  }
}
